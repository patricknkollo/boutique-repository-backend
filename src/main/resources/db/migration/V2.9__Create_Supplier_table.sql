Create table supplier(
                         supplierid int AUTO_INCREMENT NOT NULL PRIMARY KEY,
                         name varchar (255),
                         country varchar(255),
                         email varchar(255),
                         phone varchar(255),
                         connectionDate date
);